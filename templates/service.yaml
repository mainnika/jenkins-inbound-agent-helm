apiVersion: v1
kind: Service
metadata:
  name: {{ include "jenkins-permanent-agent-helm.fullname" . }}
  labels:
    {{- include "jenkins-permanent-agent-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jenkins-permanent-agent-helm.selectorLabels" . | nindent 4 }}
