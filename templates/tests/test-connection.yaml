apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jenkins-permanent-agent-helm.fullname" . }}-test-connection"
  labels:
    {{- include "jenkins-permanent-agent-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jenkins-permanent-agent-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
